const checkParenthesis = (parenthesis) => {
    let result = false

    const length = parenthesis.length
    for (let i = 0; i < length; i++) {
        const symbol = parenthesis[i]
        // symbol ??
        // what to do?
    }

    return false
}

module.exports = checkParenthesis

// Example of main function for run test
// Run test via `node test.js`

/*

(

result = false
// object
// string
// array
// number


 */
